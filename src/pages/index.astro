---
import Layout from "../layouts/Layout.astro"
import Card from "../components/Card.astro"
import Nav from "../components/Nav.astro"
import SearchBar from "../components/search-bar.svelte"
// import Search from "../components/Search.astro"
import "../styles/global.css"

const posts = await Astro.glob("./preguntas/*.md")
---

<Layout>
	<div
		class="absolute top-0 bottom-0 z-[-2] h-screen max-h-[650px] w-full bg-[#0c111c]
      bg-[radial-gradient(ellipse_80%_80%_at_50%_-20%,rgba(217,216,255,0.5),rgba(255,255,255,0.9))]
      dark:bg-[radial-gradient(ellipse_80%_80%_at_50%_-20%,rgba(120,119,198,0.3),rgba(255,255,255,0))]"
	>
	</div>
	<Nav />
	<main class="mt-24 w-full max-w-[100ch] mx-auto p-4">
		<section class="py-5">
			<div class="flex">
				<div class="max-w-xl">
					<h1 class="text-5xl font-bold font-[SpaceGrotesk] my-5">Astro.js Wiki</h1>
					<p>
						Astro.js wiki es un sitio donde <strong
							class="text-2xl animate-fade-in-right animate-duration-600 font-bold from-blue-400 to-blue-200 bg-gradient-to-r bg-clip-text text-transparent"
							>podrás resolver todas tus dudas</strong
						> sobre el framework de JavaScript Astro.js. Verás las preguntas mas frecuentes
						de Astro.js. Routing 🌐 Rendering 🦚 Islands 🏝️ Markdown ⬇️. Obtendrás todos
						las lecciones de forma gratuita en Español (es-ES).
					</p>
				</div>
				<img
				src="favicon.svg"
				alt=""
				class="max-w-[200px] drop-shadow-[4px_4px_5rem_white]"
				/>
			</div>
			<SearchBar posts={posts} client:load />
		</section>
		<section id="articles" class="w-full z-10">
			<ol
				class="w-full mx-auto grid grid-cols-3 max-md:grid-cols-2 max-sm:grid-cols-1 gap-5 mt-6 z-10 relative"
			>
				{
					posts.map((post) => (
						<Card
							level={post.frontmatter.difficulty}
							title={post.frontmatter.title}
							body={post.frontmatter.description}
							href={post.url}
							id={post.frontmatter.post_id}
						/>
					))
				}
			</ol>
		</section>
	</main>
	<footer>
		<div class="w-full max-w-[100ch] m-auto p-4 text-center">
			<p class="text-gray-400">
				Hecho con <span class="text-red-500">❤️</span> por <a href="https://erik.pages.dev"
					class="text-blue-400 hover:underline">Erik</a>
			</p>
			<p class="text-gray-400 py-2">
				<a href="https://github.com/erixweb/astrowiki"
					class="text-blue-400 hover:underline">GitHub</a>
			</p>
	</footer>
</Layout>
